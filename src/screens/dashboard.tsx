import React from 'react';
import { Container, Alert } from 'react-bootstrap';

const Dashboard: React.FC = ({

}) => (
  <Container>
    {' '}
    <Alert>Welcome to USP Dashboard</Alert>
  </Container>
);

export default Dashboard;
